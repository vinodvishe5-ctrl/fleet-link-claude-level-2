# src/data/

The in-memory store (`store.js`) and the fixed FSD §7 sample set (`seed.js`).

**Empty as of Module 2.B (scaffold).** Created in **Module 2.C**, alongside the models. This README holds
the folder in git.
