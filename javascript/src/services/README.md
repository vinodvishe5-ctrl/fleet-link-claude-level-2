# src/services/

Business rules live here; routes stay thin and call services.

As of Module 2.B this holds only `metaService.js` (non-domain, **no business logic** — it just proves the
route → service layering). The domain services with the FSD §5 rules arrive in **Module 2.D**.
