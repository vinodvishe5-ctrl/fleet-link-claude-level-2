# Data/

The in-memory store (`FleetStore`, a seeded singleton) and `SeedData` (the fixed FSD §7 sample set).

**Empty as of Module 2.B (scaffold).** Filled in **Module 2.C**, alongside the models. This README holds
the folder in git until then.
