# Dtos/

Request/response DTOs — the API never exposes `Models/` directly.

As of Module 2.B this holds only `MetaDto` (non-domain, for the `/api/meta` scaffold slice). The domain
DTOs (`WorkOrderDto`, `CreateWorkOrderRequest`, …) arrive in **Module 2.D**.
